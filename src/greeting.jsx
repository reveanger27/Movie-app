function Greeting ({nama, kota}) {
    return (
        <div>
            <h2>Nama Gw adalah {nama}</h2>
            <p>Gw berasal dari kota {kota}</p>
        </div>
    );
}

export default Greeting;